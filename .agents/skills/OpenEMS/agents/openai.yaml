interface:
  display_name: "OpenEMS"
  short_description: "Repo-specific patterns and workflows for OpenEMS"
  default_prompt: "Use the OpenEMS repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true