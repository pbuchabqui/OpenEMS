# Plan: Update README.md with Verified Features

## Purpose
Update the README.md document to reflect the verified features mentioned in the task tool output:
1. Status bits expanded to 16-bit (bytes 6-7 for CAN frame)
2. ETB PWM on TIM1/PA8 with H-bridge DIR/EN on PB14/PB15
3. Torque manager correctly gates ETB enable on APP faults vs ETB faults
4. Dual-sensor throttle plausibility working properly
5. X-τ autocal learning correctly blocked by 60s interval and ±20 STFT deadband

## Steps

### 1. Verify Current README Content
- Check existing ETB section (lines 219-226)
- Check existing X-τ Autocalibracao section (lines 228-233)
- Identify appropriate locations for new information

### 2. Add Status Bits Documentation
- Add information about 16-bit status bits expansion
- Mention that status bits are transmitted in CAN frame bytes 6-7
- Reference the STATUS_* constants in src/app/status_bits.h

### 3. Verify/Update ETB Section
- Confirm ETB PWM, DIR, EN pinout is correctly documented
- Add information about torque manager gating behavior if appropriate

### 4. Add Torque Manager Gating Information
- Document how APP faults vs ETB faults are handled differently
- APP faults: limit ETB opening (limp home) but don't disable enable request
- ETB faults: disable ETB enable request entirely

### 5. Add Dual-Sensor Throttle Plausibility Information
- Document APP plausibility checking (comparing APP1 vs APP2)
- Document ETB TPS plausibility checking (comparing ETB_TPS1 vs ETB_TPS2)
- Reference plausibility fault bits: THROTTLE_FAULT_APP_PLAUS and THROTTLE_FAULT_ETB_PLAUS

### 6. Add/Update X-τ Autocalibracao Section
- Confirm 60s interval blocking is documented (kLearnIntervalMs = 60000u)
- Confirm ±20 STFT deadband is documented (no learning when |STFT| <= 20)
- Update if any information is missing or unclear

### 7. Verify Changes
- Ensure README remains the single source of truth
- Verify technical accuracy against actual implementation
- Maintain consistent style with existing documentation

## Implementation Notes
- All changes must be made to README.md only (per project policy)
- No parallel Markdown documents should be created
- Technical details should be accurate and verifiable in the codebase