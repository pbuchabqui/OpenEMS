# Paths (relative to repo root) allowed to #include "hal/regs.h" under src/engine/**
# Phase B lint uses this. Remove entries only after migration PRs (out_pins / aux_gpio).
#
# PR-12 migrates ecu_sched INJ/IGN; auxiliaries fan/pump stays until PR-17 (Phase C).
src/engine/ecu_sched.cpp
src/engine/auxiliaries.cpp
